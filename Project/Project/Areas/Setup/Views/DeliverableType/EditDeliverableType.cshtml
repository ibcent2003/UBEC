@model Project.Areas.Setup.Models.DeliverableTypeViewModel
@using Project.HtmlHelpers
@{
    ViewBag.Title = "Edit Deliverable Type";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.IsGreetings = "true";
}
@section Crumbs{
    <div class="crumbs">

        <ul class="breadcrumb" id="breadcrumbs">
            <li> <i class="icon-home"></i> <a href="@Url.Action("Index", "Dashboard", new { area="Admin"})">Dashboard</a> </li>
            <li class="current"> <a title="" href="javascript: return void(0)"></a>Edit Deliverable Type</li>
        </ul>
    </div>
}

<style>
    .required:after {
        content: "*";
        font-weight: bold;
        color: red;
    }
</style>


<div class="row">
    <div class="col-md-12">
        <div class="widget box">
            <div class="widget-header">
                <h4>
                    <i class="icon-reorder"></i>
                    Edit Deliverable Type

                </h4>
            </div>
            <div class="widget-content">
                @using (Html.BeginForm("EditDeliverableType", "DeliverableType", FormMethod.Post, new { @enctype = "multipart/form-data", @class = "form-horizontal row-border" }))
                {

                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(x=>x.DeliverableTypeform.Id)

                    @*<div class="form-group">
                            @Html.Label("Document Category", new { @class = "col-md-2 control-label required" })
                            <div class="col-md-6">
                                @Html.DropDownListFor(m => Model.documentTypeform.DocumentCategoryId, new SelectList(Model.CategoryList, "Value", "Text"), "Please select Category", new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.documentTypeform.DocumentCategoryId, null, new { @style = "color:red" })
                            </div>
                        </div>*@

                    <div class="form-group">
                        @Html.Label("Deliverable Name", new { @class = "col-md-2 control-label required" })
                        <div class="col-md-6">
                            @Html.TextBoxFor(m => m.DeliverableTypeform.Name, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.DeliverableTypeform.Name, null, new { @style = "color:red" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.Label("Is Deleted?", new { @class = "col-md-2 control-label" })
                        <div class="col-md-6">
                            @Html.CheckBoxFor(m => m.DeliverableTypeform.IsDeleted)
                        </div>
                    </div>


                    <div class="form-actions">
                        <a href="@Url.Action("Index", "DeliverableType", new { area="Setup"})" class="btn btn-success btn-xs"><< Back to List </a>
                        <input type="submit" class="btn btn-warning btn-xs pull-right" value="Save" />
                    </div>
                }
            </div>

        </div>
    </div>
</div>